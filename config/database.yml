development:
  adapter: postgresql
  database: hankthecowdog
  port: 5433
  # Omit host to use Unix socket (peer auth on WSL/Linux; no password needed)

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: hankthecowdog
  port: 5433

production:
  adapter: postgresql
  database: hankthecowdog
  host: localhost
